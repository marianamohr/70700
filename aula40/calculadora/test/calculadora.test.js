const {
  calculadora,
  soma,
  subtracao,
  multiplicacao,
  divisao,
} = require("../src/index");
const assert = require("assert");

describe("Calculadora", () => {

  it('Sucesso - soma', async () => {
    // A = Arrange
    // configura os inputs
    // num1 e num2
    // configura o mock de soma
    // soma = 4

  });
});
